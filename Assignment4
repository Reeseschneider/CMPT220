public class App {
    public static void main(String[] args) throws Exception {
        int[] myArray = {10,3,295,38,20,3,4,267,2445,10,5566,87,93,17,10,2,87,267,3176,3,82};

        for (int i = 0; i < myArray.length; i++) {

            boolean alreadyChecked = false;

            // Check if this number appeared before index i
            for (int j = 0; j < i; j++) {
                if (myArray[i] == myArray[j]) {
                    alreadyChecked = true;
                    break;
                }
            }

            // Only checks for duplicates that haven't been seen before
            if (!alreadyChecked) {
                for (int k = i + 1; k < myArray.length; k++) {
                    if (myArray[i] == myArray[k]) {
                        System.out.println("Duplicate number: " + myArray[i]);
                        break;  // Makes sure that the same duplicate isn't reported twice
                        }
                    }
             }
         }
    }
}
//What I learned: I learned how to find duplicates using nested loops
//I also learned how to prevent printing the same duplicate more than once
//I also learned how the outer loops selects a number and the inner loop compares it to the other numbers in the array
